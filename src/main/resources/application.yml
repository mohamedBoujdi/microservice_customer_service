server:
  port: 8082
spring:
  application:
    name: CUSTOMER-SERVICE
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:customer-db
  cloud:
    discovery:
      enabled: false  #disable discovery service
